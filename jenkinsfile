pipeline {
    agent  any
    tool {
        maven 'maven123'
    }
    stages {
        stage('checkout') {
            steps {
                checkout scm
            }
        }
        stage('build') {
            steps {
                sh 'mvn clean install'
            }
        }
    }
}
